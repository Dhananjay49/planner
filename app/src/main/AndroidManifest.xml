<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edureka.training.zen_practice_2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".homeActvity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".taskActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".dayViewActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".subtaskActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".addNode_popupActivity"
            android:theme="@style/AppTheme.Popup"></activity>
        <activity android:name=".DatePickerActivity"
            android:theme="@style/AppTheme.PopupDatePicker"></activity>
        <activity android:name=".editNode_popupActivity"
            android:theme="@style/AppTheme.Popup"></activity>
    </application>

</manifest>